import type {NextPage} from 'next';

import Head from 'next/head';

const Home: NextPage = () => {
    return (
        <section>
            <Head>
                <title>RESERVATION</title>
            </Head>
            <h1>타이틀</h1>
        </section>
    );
};

export default Home;
